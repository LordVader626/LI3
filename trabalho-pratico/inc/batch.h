#ifndef batch_h
#define batch_h


/**
 * @brief Função de modo batch.
 * 
 * Esta função é inicia a execução do programa em modo batch
 * 
 * @param files_path string do caminho para os ficheiros a ser usados.
 * @param data_input string dos dados(queries).
 */
void batch(char *files_path, char *data_input);

#endif